#include <iostream>
#include "grid.h"

int main()
{
  // read the settings
  // load the grid
  grid dnsgrid;

  // load the fields
  //
  // start the time loop
  // 1. boundary conditions
  // 2. advection
  // 3. diffusion
  // 4. gravity
  // 5. large scale forcings
  // 6. pressure
  std::cout << "Hello world!" << std::endl;
  return 0;
}

